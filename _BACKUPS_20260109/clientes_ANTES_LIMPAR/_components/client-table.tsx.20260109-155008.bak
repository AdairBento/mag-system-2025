'use client'

import { Eye, Edit, Trash2 } from 'lucide-react'
import { Pagination } from '@/components/common/pagination'

export function ClientTable() {
  // TODO: Implementar lógica de fetch e estado
  const mockData = [
    {
      id: '1',
      type: 'PF' as const,
      name: 'João Silva',
      cpf: '123.456.789-00',
      phone: '(31) 99999-9999',
      city: 'Betim',
      status: 'ATIVO',
    },
  ]

  return (
    <div className="rounded-lg border border-gray-200 bg-white">
      <div className="overflow-x-auto">
        <table className="w-full">
          <thead className="border-b bg-gray-50">
            <tr>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">Avatar</th>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">Nome</th>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">CPF/CNPJ</th>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">Telefone</th>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">Cidade</th>
              <th className="px-4 py-3 text-left text-sm font-semibold text-gray-900">Status</th>
              <th className="px-4 py-3 text-center text-sm font-semibold text-gray-900">Ações</th>
            </tr>
          </thead>
          <tbody className="divide-y divide-gray-200">
            {mockData.map((client) => (
              <tr key={client.id} className="hover:bg-gray-50">
                <td className="px-4 py-3 text-center">{client.type === 'PF' ? '👤' : '🏢'}</td>
                <td className="px-4 py-3 text-sm text-gray-900">{client.name}</td>
                <td className="px-4 py-3 text-sm text-gray-600">{client.cpf}</td>
                <td className="px-4 py-3 text-sm text-gray-600">{client.phone}</td>
                <td className="px-4 py-3 text-sm text-gray-600">{client.city}</td>
                <td className="px-4 py-3">
                  <span className="inline-flex rounded-full bg-green-100 px-2 py-1 text-xs font-medium text-green-800">
                    🟢 {client.status}
                  </span>
                </td>
                <td className="px-4 py-3">
                  <div className="flex justify-center gap-2">
                    <button className="rounded p-1 text-blue-600 hover:bg-blue-50">
                      <Eye size={18} />
                    </button>
                    <button className="rounded p-1 text-gray-600 hover:bg-gray-50">
                      <Edit size={18} />
                    </button>
                    <button className="rounded p-1 text-red-600 hover:bg-red-50">
                      <Trash2 size={18} />
                    </button>
                  </div>
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>

      <Pagination
        currentPage={1}
        totalPages={1}
        totalItems={1}
        itemsPerPage={10}
        onPageChange={() => {}}
      />
    </div>
  )
}
